{
  "name": "BMF Bridge",
  "description": "Generic socket-backed BMF command/event bridge for Omegga plugins and BMF Desktop event inspection.",
  "author": "BMF",
  "config": {
    "enabled": {
      "description": "Enable the bridge plugin.",
      "default": true,
      "type": "boolean"
    },
    "runtimeDir": {
      "description": "Path to Mods/BMF/runtime. If blank, OMEGGA_BMF_RUNTIME_DIR or the standard Omegga-managed BMF runtime path is used.",
      "default": "",
      "type": "string"
    },
    "socketPath": {
      "description": "Path to Mods/BMF/runtime/socket.json. If blank, runtimeDir/socket.json is used.",
      "default": "",
      "type": "string"
    },
    "preferredTransport": {
      "description": "Command/event transport. Must remain socket; disconnected sockets are reported as unhealthy.",
      "default": "socket",
      "type": "string"
    },
    "socketRole": {
      "description": "Client role sent to the BMF socket broker.",
      "default": "plugin",
      "type": "string"
    },
    "socketSource": {
      "description": "Source name sent in socket hello, command, and diagnostics envelopes.",
      "default": "omegga.bmf-bridge",
      "type": "string"
    },
    "maxRecords": {
      "description": "Maximum normalized records retained in memory for Omegga plugin consumers and BMF Desktop inspection.",
      "default": 500,
      "type": "number"
    },
    "commandTimeoutMs": {
      "description": "Timeout for BMF command responses over the socket bridge.",
      "default": 5000,
      "type": "number"
    },
    "socketReconnectMs": {
      "description": "Delay before reconnecting to the BMF socket broker after a disconnect. Set to 0 to disable reconnects.",
      "default": 2500,
      "type": "number"
    },
    "statusWriteIntervalMs": {
      "description": "Interval for writing bmf-bridge-status.json. Set to 0 to write only on status changes.",
      "default": 5000,
      "type": "number"
    },
    "redactPrivateIpsOnExport": {
      "description": "Also redact private IPv4 addresses in exported or requested sanitized records.",
      "default": false,
      "type": "boolean"
    }
  },
  "commands": [
    {
      "name": "bmfbridge",
      "description": "Show BMF bridge socket status and command/event transport health."
    }
  ]
}
