# Deprecated

`BrickAssetPlacementGuard` is no longer deployed. Brickadia now provides the
brick placement restrictions used by the server, so maintaining a second BMF
policy would create conflicting enforcement paths.

The plugin remains here only for historical validation and reference.
